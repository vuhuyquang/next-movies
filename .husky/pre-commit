#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Kiểm tra nếu đang trong quá trình merge commit
if git rev-parse --verify MERGE_HEAD >/dev/null 2>&1; then
  echo "Skipping pre-commit hook during merge commit"
  exit 0
fi

# Nếu không phải merge commit, chạy lint-staged
npx --no-install lint-staged
